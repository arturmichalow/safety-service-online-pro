import { prisma } from '../../../lib/prisma';
import { currentUser } from '../../../lib/auth';
export async function POST(req){const user=currentUser(); if(!user)return Response.json({error:'Unauthorized'},{status:401}); const b=await req.json(); const entry=await prisma.workEntry.create({data:{date:new Date(b.date),companyId:b.companyId,userId:user.id,orderNumber:b.orderNumber||null,type:b.type||'inne',title:b.title,description:b.description||null,minutes:Number(b.minutes||0),travelMinutes:Number(b.travelMinutes||0)}}); await prisma.auditLog.create({data:{userId:user.id,action:'CREATE',entity:'WorkEntry',entityId:entry.id,after:entry}}); return Response.json(entry)}
